# Legacy migrations

Supabase migrations are maintained exclusively in `supabase/migrations/`.
Do not place SQL migrations in this directory. The canonical migration history is the filename/version set under `supabase/migrations/`.
